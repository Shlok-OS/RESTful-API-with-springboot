# RESTful-API-with-springboot
A full CRUD REST API for managing tasks, built with Spring Boot, Spring Web, and Spring Data JPA. Includes DTO-based request/response validation, centralized exception handling with correct HTTP status codes, Flyway-managed PostgreSQL migrations, a JUnit 5 + Mockito test suite, and a ready-to-import Postman collection.
